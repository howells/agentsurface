// next-env.d.ts is generated and git-ignored, so CI typechecks without it.
// This keeps static image imports (the glossary SVGs) typed on a fresh checkout.
/// <reference types="next/image-types/global" />
